import { tokens } from "@/lib/theme";

export { tokens };

/** Font stacks used in the section-details timeline. */
export const fonts = {
  label: '"Space Grotesk", sans-serif',
  body: '"Inter", sans-serif',
  mono: '"Space Mono", "Courier New", monospace',
} as const;
